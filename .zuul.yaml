---
- project:
    merge-mode: squash-merge
    check:
      jobs:
        - build-sphinx-docs
        - yamllint
        - ansible-lint
    gate:
      jobs:
        - build-sphinx-docs
        - yamllint
        - ansible-lint
    periodic-daily:
      jobs:
        - yamllint
        - ansible-lint

